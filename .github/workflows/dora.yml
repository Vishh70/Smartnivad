name: DORA Metrics

on:
  schedule:
    # Run daily at midnight
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  dora-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Calculate DORA Metrics (Placeholder)
        run: echo "DORA metrics collection temporarily disabled until a free/maintained action is selected."
